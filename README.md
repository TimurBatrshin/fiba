# FIBA - Система организации турниров по баскетболу

Полноценная платформа для организации и управления баскетбольными турнирами, регистрации команд и управления рекламой.

## 📋 Содержание

- [Обзор](#обзор)
- [Функциональность](#функциональность)
- [Технологический стек](#технологический-стек)
- [Архитектура проекта](#архитектура-проекта)
- [Предварительные требования](#предварительные-требования)
- [Установка и настройка](#установка-и-настройка)
- [Запуск проекта](#запуск-проекта)
- [Структура проекта](#структура-проекта)
- [Разработка и тестирование](#разработка-и-тестирование)
- [API документация](#api-документация)
- [Развертывание](#развертывание)
- [Конфигурация окружения](#конфигурация-окружения)
- [Участие в проекте](#участие-в-проекте)
- [Лицензия](#лицензия)

## 🌟 Обзор

FIBA - это современная веб-платформа для организации и проведения баскетбольных турниров. Приложение разработано с использованием React для фронтенда и Spring Boot (Java) для бэкенда. FIBA предоставляет полный набор инструментов для организаторов турниров, команд, игроков и рекламодателей.

## 🚀 Функциональность

### Для пользователей:
- Регистрация и авторизация через JWT-токены
- Профили пользователей с персональной статистикой
- Просмотр предстоящих и текущих турниров
- Регистрация и участие в турнирах
- Персональный календарь мероприятий

### Для организаторов:
- Создание и управление турнирами
- Подтверждение регистраций команд
- Автоматическое формирование сетки турнира 
- Управление расписанием матчей
- Публикация результатов и статистики

### Для рекламодателей:
- Размещение рекламы на турнирах
- Статистика просмотров и кликов
- Взаимодействие с бизнес-партнерами

### Статистика и аналитика:
- Детальная статистика игроков и команд
- Рейтинги лучших игроков
- Графическое представление данных
- История турниров и матчей

## 💻 Технологический стек

### Frontend:
- **Язык программирования**: TypeScript
- **Основной фреймворк**: React 18
- **Роутинг**: React Router v7
- **Управление состоянием**: React Context API + Hooks
- **HTTP-клиент**: Axios
- **Стилизация**: SCSS
- **UI-компоненты**: React Big Calendar, FontAwesome
- **Сборка**: Webpack 5

### Backend:
- **Язык программирования**: Java 21
- **Фреймворк**: Spring Boot 3
- **ORM**: Spring Data JPA (Hibernate)
- **Безопасность**: Spring Security, JWT
- **Валидация**: Bean Validation
- **REST API**: Spring Web MVC
- **Email**: Spring Mail

### База данных:
- PostgreSQL 12+
- Sequelize для ORM (в тестовом API)

### Инфраструктура:
- **Аутентификация**: JWT
- **Управление файлами**: Multer (для тестового API), Spring FileStorage
- **Контейнеризация**: Поддержка Docker (в процессе реализации)
- **CI/CD**: GitHub Actions (планируется)

## 🏗️ Архитектура проекта

Проект построен на микросервисной архитектуре, состоящей из:

1. **Клиентское приложение** - React SPA
2. **API-сервер** - RESTful API на Spring Boot
3. **База данных** - PostgreSQL
4. **Сервис авторизации** - JWT-based
5. **Сервис хранения файлов** - локальное хранилище

## 📝 Предварительные требования

Для работы с проектом необходимо:

- Node.js (v16.0+)
- npm (v7.0+) или yarn (v1.22+)
- Java JDK 21
- PostgreSQL 12+
- Git

## 🔧 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/fiba.git
cd fiba
```

### 2. Настройка переменных окружения

```bash
# Создание .env файла из примера
cp .env.example .env
# Отредактируйте файл .env, указав ваши параметры подключения
```

### 3. Установка зависимостей Frontend

```bash
# Установка зависимостей
npm install
```

### 4. Установка зависимостей Backend

```bash
cd api-java
./mvnw install
```

### 5. Настройка базы данных

```bash
# Создайте базу данных PostgreSQL
createdb -U postgres fiba_db

# Настройте доступ в .env и application.properties
```

## 🖥️ Запуск проекта

### Режим разработки

Запуск серверной части:
```bash
cd api-java
./mvnw spring-boot:run
```

Запуск клиентской части:
```bash
# В корневой директории проекта
npm run client
```

Запуск тестового API сервера (mock):
```bash
npm run server
```

Запуск всего проекта одновременно:
```bash
# Требуется установка concurrently
npm start
```

## 📁 Структура проекта

```
fiba/
│
├── src/                     # Frontend исходный код (React/TypeScript)
│   ├── app.tsx              # Точка входа приложения
│   ├── index.tsx            # Рендер-точка React
│   ├── components/          # Переиспользуемые компоненты
│   ├── pages/               # Компоненты страниц
│   ├── hooks/               # Кастомные React-хуки
│   ├── context/             # React Context providers
│   ├── services/            # Сервисы для взаимодействия с API
│   ├── utils/               # Вспомогательные функции
│   ├── interfaces/          # TypeScript типы и интерфейсы
│   ├── styles/              # Глобальные стили SCSS
│   └── assets/              # Статические ресурсы
│
├── api-java/                # Backend исходный код (Spring Boot)
│   ├── src/                 # Java код
│   │   ├── main/java/com/fiba/api/
│   │   │   ├── controller/  # REST контроллеры
│   │   │   ├── model/       # Модели данных
│   │   │   ├── repository/  # Data Access Objects
│   │   │   ├── service/     # Бизнес-логика
│   │   │   ├── config/      # Конфигурация
│   │   │   ├── security/    # Безопасность
│   │   │   └── exception/   # Обработка ошибок
│   │   └── resources/       # Ресурсы и конфигурация
│   ├── pom.xml              # Maven зависимости
│   └── uploads/             # Директория для загрузки файлов
│
├── stubs/                   # Тестовое API для разработки
│   └── api/                 # Express.js mock API
│
├── public/                  # Публичные статические файлы
├── .env.example             # Пример файла окружения
├── package.json             # NPM зависимости и скрипты
└── README.md                # Документация проекта
```

## 🧪 Разработка и тестирование

### Рекомендации по разработке

1. Следуйте принципам компонентного подхода при работе с React
2. Используйте TypeScript для типизации всех компонентов и функций
3. Реализуйте юнит-тесты для критических компонентов
4. Следуйте REST-принципам при разработке API

### Команды для разработки

- `npm run client` - запуск фронтенда в режиме разработки
- `npm run server` - запуск тестового API сервера
- `npm run build` - сборка проекта для разработки
- `npm run build:prod` - сборка проекта для продакшена
- `npm run clean` - очистка сборочных файлов

## 📃 API документация

REST API проекта следует стандартным REST-практикам:

* `GET /api/tournaments` - получение списка турниров
* `POST /api/tournaments` - создание нового турнира
* `GET /api/tournaments/{id}` - получение турнира по ID
* `PUT /api/tournaments/{id}` - обновление турнира
* `DELETE /api/tournaments/{id}` - удаление турнира

Полная документация API доступна через Swagger после запуска серверной части по адресу:
```
http://localhost:9090/swagger-ui.html
```

## 🚢 Развертывание

### Продакшн-сборка

```bash
# Сборка фронтенда
npm run build:prod

# Сборка бэкенда
cd api-java
./mvnw package
```

### Деплой на сервер с Яндекс.Облаком

1. Подготовка сервера:
   ```bash
   # Установите необходимое ПО
   sudo apt update
   sudo apt install openjdk-21-jre nginx
   ```

2. Настройка базы данных в Яндекс.Облаке:
   - Подключитесь к вашему кластеру:
   ```bash
   psql "host=rc1a-345wqvguutz56h32.mdb.yandexcloud.net port=6432 sslmode=require dbname=postgres user=Timur007 password=qwerty123"
   ```
   - Проверьте наличие базы данных streetball или создайте её:
   ```sql
   CREATE DATABASE streetball IF NOT EXISTS;
   ```

3. Настройка файлов конфигурации:
   - Отредактируйте `api-java/src/main/resources/application.properties`:
   ```properties
   spring.datasource.url=jdbc:postgresql://rc1a-345wqvguutz56h32.mdb.yandexcloud.net:6432/streetball
   spring.datasource.username=Timur007
   spring.datasource.password=qwerty123
   spring.datasource.hikari.ssl-mode=require
   ```

4. Деплой бэкенда:
   ```bash
   # Копируйте JAR-файл на сервер
   scp api-java/target/api-1.0.0.jar user@your-server:/path/to/deployment/
   
   # Создайте systemd сервис для автозапуска
   sudo nano /etc/systemd/system/fiba-backend.service
   ```
   
   Содержимое файла сервиса:
   ```
   [Unit]
   Description=FIBA Backend Service
   After=network.target
   
   [Service]
   User=your-user
   WorkingDirectory=/path/to/deployment
   ExecStart=java -jar api-1.0.0.jar
   SuccessExitStatus=143
   TimeoutStopSec=10
   Restart=on-failure
   RestartSec=5
   
   [Install]
   WantedBy=multi-user.target
   ```
   
   ```bash
   # Активация и запуск сервиса
   sudo systemctl daemon-reload
   sudo systemctl enable fiba-backend
   sudo systemctl start fiba-backend
   ```

5. Деплой фронтенда:
   ```bash
   # Копируйте файлы фронтенда на сервер
   scp -r build/* user@your-server:/var/www/fiba/
   
   # Настройте Nginx
   sudo nano /etc/nginx/sites-available/fiba
   ```
   
   Содержимое конфигурации Nginx:
   ```
   server {
       listen 80;
       server_name your-domain.com www.your-domain.com;
       root /var/www/fiba;
       index index.html;
       
       # Сжатие
       gzip on;
       gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
       
       # Статические файлы
       location /static/ {
           expires 1y;
           add_header Cache-Control "public, max-age=31536000, immutable";
       }
       
       # API прокси
       location /api/ {
           proxy_pass http://localhost:9090/api/;
           proxy_http_version 1.1;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
       
       # Swagger UI
       location /swagger-ui/ {
           proxy_pass http://localhost:9090/swagger-ui/;
           proxy_http_version 1.1;
           proxy_set_header Host $host;
       }
       
       # Для React Router
       location / {
           try_files $uri $uri/ /index.html;
       }
   }
   ```
   
   ```bash
   # Активация сайта
   sudo ln -s /etc/nginx/sites-available/fiba /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl restart nginx
   ```

6. Настройка SSL (опционально, но рекомендуется):
   ```bash
   # Установка Certbot
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com -d www.your-domain.com
   ```

7. Настройка мониторинга (опционально):
   ```bash
   # Проверка статуса приложения
   sudo systemctl status fiba-backend
   
   # Просмотр логов
   sudo journalctl -u fiba-backend -f
   ```

## ⚙️ Конфигурация окружения

Основные параметры конфигурации в `.env`:

```
# API URL
REACT_APP_API_URL=http://localhost:9090/api

# Database
DB_HOST=rc1a-345wqvguutz56h32.mdb.yandexcloud.net
DB_PORT=6432 # стандартный порт PostgreSQL в Яндекс.Облаке
DB_USER=Timur007
DB_PASSWORD=qwerty123
DB_NAME=streetball

# JWT
JWT_SECRET=your_secret_key
JWT_EXPIRATION=86400

# Mail
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=your_email@example.com
MAIL_PASSWORD=your_password
```

## 🤝 Участие в проекте

Мы приветствуем вклад в развитие проекта! Если вы хотите внести свой вклад:

1. Форкните репозиторий
2. Создайте ветку для ваших изменений (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте изменения в ваш форк (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT - подробности см. в файле LICENSE. 